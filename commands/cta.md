---
description: "Show the call-to-action patterns from your analysed reels — what asks actually convert. Free, never spends credits"
argument-hint: "[@handle, a tag name, or a question about CTAs]"
---

Show CTA patterns from Reach Machine. **Reads only — never spend.**

Narrow to: $ARGUMENTS

1. `get_analysis_coverage` FIRST. CTA data only exists for **analysed** reels. Thin coverage
   means a low-confidence answer, and you must say so.
2. `get_cta_library` for the patterns. Scope one account with `usernames=[handle]`, or a
   hand-picked set via `set_data_selection` with `post_urls`.
3. Apply the skill's PLAYBOOK rigor rules (Step 3, Step 7): medians not means, sample size
   stated, every claim labelled DATA-DRIVEN / DATA-INFERRED / JUDGMENT.
4. **Read the CTA rules before advising.** A CTA that suits a 50k account can cost a small
   account its reach — see the PLAYBOOK's stage translation (Step 4) and `rules/funnel.md`.
   A keyword-DM ask needs the funnel assets to actually exist first.
5. If you scoped with `set_data_selection`, **always** `clear_data_selection` before finishing.

**Hard limit:** never call a spend or destructive tool here, and never call Apify. If the
answer needs new analysis, point at `/rm-social-media-manager:watch-video`.
